const layoutStyle = () => ({
    layout: {
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        marginTop: "auto",
        marginBottom: "0",
        flexDirection: "column",
        width: "100%",
    },

});

export default layoutStyle;
